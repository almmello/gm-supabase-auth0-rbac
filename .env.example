# Debug
# Habilita/desabilita logs de depuração
NEXT_PUBLIC_DEBUG_MODE=true

# Auth0
# Gerar com: openssl rand -hex 32
# Este é um segredo usado para criptografar os cookies da sessão
AUTH0_SECRET=your-auth0-secret

# URL base da sua aplicação (desenvolvimento: localhost, produção: seu domínio)
AUTH0_BASE_URL=http://localhost:3000

# URL do seu tenant Auth0 (obtida no dashboard do Auth0)
# Formato: https://[seu-tenant].[região].auth0.com
AUTH0_ISSUER_BASE_URL=https://your-tenant.region.auth0.com

# Credenciais obtidas no dashboard do Auth0 > Applications > Your App > Settings
AUTH0_CLIENT_ID=your-client-id
AUTH0_CLIENT_SECRET=your-client-secret

# Identificador da API no Auth0 (obtido em Auth0 > APIs > Settings > 
# Usado para validar tokens de acesso
AUTH0_AUDIENCE=your-api-audience

# Namespace para claims customizados no token JWT
# Usado para acessar as roles do usuário no token
# Pode ser igual ao AUTH0_ISSUER_BASE_URL sem o https://
NEXT_PUBLIC_AUTH0_NAMESPACE=your-namespace

# Supabase
# URL do seu projeto Supabase (obtida no dashboard do Supabase)
NEXT_PUBLIC_SUPABASE_URL=your-url

# Chave pública anônima do Supabase (obtida no dashboard do Supabase)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key

# Segredo usado para assinar tokens JWT do Supabase
# Gerar com: openssl rand -hex 32
SUPABASE_SIGNING_SECRET=your-jwt-secret
